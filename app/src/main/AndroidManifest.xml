<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ru.ingos.digitalmedicine">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme.NoActionBar">
        <meta-data
                android:name="com.google.android.gms.version"
                android:value="@integer/google_play_services_version" />
        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="AIzaSyBFcDEjaK-VlvY-7NEsVt-efWuec0K3dvY"/>

        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.activities.SpecialtyActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.ClinicInfoActivity"/>
        <activity android:name=".ui.activities.FragmentHolderActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.NewRegistryActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.AddRecipeActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.MedCardActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.RegistryInfoActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.SelectRegistryActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.DoctorsActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.InsuranceServicesInfoActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.PersonalDataActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.activities.StatsActivity" android:theme="@style/AppTheme"/>
    </application>

</manifest>